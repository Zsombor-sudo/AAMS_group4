
world:
  height: 20  # the height of the world
  width: 20  # the width of the world
  step_time: 0.1  # 10Hz calculate each step
  sample_time: 0.1  # 10 Hz for render and data extraction 
  offset: [0, 0] # the offset of the world on x and y 

robot:
  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [10, 15, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [5, 10, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [10, 5, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [0, 5, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [16, 15, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [5, 5, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  
  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [16, 2, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true
  

  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [6, 17, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true

  
  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [8, 3, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true


  - kinematics: {name: 'diff'}  # omni, diff, acker
    shape: {name: 'circle', radius: 0.2}  # radius
    state: [16, 10, 0]  # x, y, theta
    color: 'g'  # green
    goal: [[10, 10, 0]]
    plot:
      show_trail: true
