world:
  height: 25  # the height of the world
  width: 25   # the width of the world
  # step_time: 0.01  # 100Hz calculate each step
  step_time: 0.1  # 10Hz calculate each step
  sample_time: 0.1  # 10 Hz for render and data extraction 
  offset: [0, 0] # the offset of the world on x and y 
  collision_mode: 'stop'  # 'stop', 'unobstructed',  
  control_mode: 'auto'  # 'keyboard', 'auto'

robot:
  - number: 5
    distribution: {name: 'circle', radius: 4.0, center: [5, 5]}  
    kinematics: {name: 'diff'}
    shape: 
      - {name: 'circle', radius: 0.2}  # radius
    # behavior: {name: 'follow_leader_line'}
    # behavior: {name: 'follow_leader'}
    behavior: {name: 'elect_leader_line'}
    vel_min: [-6, -3.0]
    vel_max: [6, 3.0]
    color: ['red', 'green', 'orange', 'purple', 'yellow', 'cyan', 'magenta', 'lime', 'pink', 'brown'] 
    fov: 6.28
    fov_radius: 2
    plot:
      show_fov: true
      show_trail: true
      show_goal: false
      trail_fill: true
      trail_alpha: 0.2
      show_trajectory: false
      keep_trail_length: 15
    unobstructed: True

    